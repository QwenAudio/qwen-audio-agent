MEMCODE_API_URL=https://memory.memcode.in
# Create this key with "Qwen Audio Agent" selected in the Memcode dashboard.
MEMCODE_API_KEY=your_memcode_api_key

# Keep the example isolated from the regular Gateway configuration.
AGENT_PROTOCOL=none
QWAUDIO_CONFIG_DIR=.qwen-audio/runtime

# The personal Memcode credential is deliberately bound to exactly one Gateway
# owner. Leave both values at their defaults for a local personal Gateway.
QWEN_AUDIO_AGENT_PERSONAL_OWNER_ID=user_personal
# PORT=3102
